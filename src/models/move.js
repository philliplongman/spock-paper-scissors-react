class Move {
  constructor(name, beats, image, winMessage, winSound, loseSound) {
    this.name = name
    this.beats = beats
    this.image = image
    this.winMessage = winMessage
    this.winSound = winSound
    this.loseSound = loseSound
  }
}


export default Move
